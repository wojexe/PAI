extends ../layouts/global.pug

block content
  h1 #{title} 🔮

  if capsules.length === 0
    blockquote Nie ma obecnie żanych kapsuł
  else
    ul.capsule_list
      each capsule in capsules
        - var publishDate = new Date(capsule.publishDate)
        - var notYetAvailabe = publishDate > new Date()

        if notYetAvailabe
          li 🔒 Kapsuła #{capsule.id}
        else
          li: a(href=`/capsules/${capsule.id}`) Kapsuła #{capsule.id}

  a(href="/capsules/new") Stwórz własną kapsułę
